version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: task-management-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    container_name: task-management-frontend
    depends_on:
      - backend
    ports:
      - "5173:80"
